#include<iostream>
#include<string>
using namespace std;
struct now{
	int len;
	int num;
}; 
int main()
{
	string str;
 	while(1)
    {
    	getline(cin,str);
        if(str.empty())
			break;
		
	}
	return 0;
}
